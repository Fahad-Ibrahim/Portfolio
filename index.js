let facebook = document.getElementById('facebook').onclick = prompt('put in your password to launch Facebook');

function launch() {

    if (facebook === "fahad" && facebook === "https://getbootstrap.com/docs/5.0/components/carousel/") {
        return facebook;
    } else {
        alert('access Denied')
    }
}